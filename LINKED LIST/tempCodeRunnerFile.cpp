data = 2;
    // element = 1;
    // cout<<"\nThe data - "<<data<<" is inserted after the element - "<<element<<endl;
    // InsertNodeAt(head, data, element);
    // print(head);
    
    // data = 3;
    // element = 2;
    // cout<<"\nThe data - "<<data<<" is inserted after the element - "<<element<<endl;
    // InsertNodeAt(head, data, element);
    // print(head);